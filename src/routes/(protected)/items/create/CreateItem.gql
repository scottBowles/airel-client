mutation CreateItem(
	$name: String!
	$description: String
	$markdownNotes: String
	$imageIds: [String!]
) {
	createItem(
		input: {
			name: $name
			description: $description
			markdownNotes: $markdownNotes
			imageIds: $imageIds
		}
	) {
		... on Item {
			id
		}
	}
}
