mutation CreatePlace(
	$name: String!
	$description: String
	$markdownNotes: String
	$imageIds: [String!]
) {
	createPlace(
		input: {
			name: $name
			description: $description
			markdownNotes: $markdownNotes
			imageIds: $imageIds
		}
	) {
		... on Place {
			id
		}
	}
}
