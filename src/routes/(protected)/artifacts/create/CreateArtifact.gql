mutation CreateArtifact(
	$name: String!
	$description: String
	$markdownNotes: String
	$imageIds: [String!]
) {
	createArtifact(
		input: {
			name: $name
			description: $description
			markdownNotes: $markdownNotes
			imageIds: $imageIds
		}
	) {
		... on Artifact {
			id
		}
	}
}
