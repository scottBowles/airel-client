mutation CreateArtifact(
	$name: String!
	$description: String
	$markdownNotes: String
	$imageIds: [String!]
	$logs: [GlobalID!]
) {
	createArtifact(
		input: {
			name: $name
			description: $description
			markdownNotes: $markdownNotes
			imageIds: $imageIds
			logs: $logs
		}
	) {
		... on Artifact {
			id
		}
	}
}
