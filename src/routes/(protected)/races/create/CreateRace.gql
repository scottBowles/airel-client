mutation CreateRace(
	$name: String!
	$description: String
	$markdownNotes: String
	$imageIds: [String!]
) {
	createRace(
		input: {
			name: $name
			description: $description
			markdownNotes: $markdownNotes
			imageIds: $imageIds
		}
	) {
		... on Race {
			id
		}
	}
}
