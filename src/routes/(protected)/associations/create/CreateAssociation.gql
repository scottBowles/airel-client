mutation CreateAssociation(
	$name: String!
	$description: String
	$markdownNotes: String
	$imageIds: [String!]
) {
	createAssociation(
		input: {
			name: $name
			description: $description
			markdownNotes: $markdownNotes
			imageIds: $imageIds
		}
	) {
		... on Association {
			id
		}
	}
}
