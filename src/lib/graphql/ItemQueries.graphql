query items {
	items {
		edges {
			node {
				id
				name
				description
				armor {
					acBonus
				}
				weapon {
					attackBonus
				}
				equipment {
					briefDescription
				}
			}
		}
	}
}

query itemById($id: ID!) {
	item(id: $id) {
		id
		name
		description
		armor {
			acBonus
		}
		weapon {
			attackBonus
		}
		equipment {
			briefDescription
		}
	}
}
