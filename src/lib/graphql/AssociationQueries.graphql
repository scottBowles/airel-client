query associations {
	associations {
		edges {
			node {
				id
				name
				description
				imageId
				thumbnailId
			}
		}
	}
}

query associationById($id: ID!) {
	association(id: $id) {
		id
		name
		description
		imageId
		thumbnailId
	}
}
