query placeById($id: ID!) {
	place(id: $id) {
		id
		name
		description
		imageIds
		thumbnailId
		markdownNotes
		lockUser {
			id
			username
		}
		lockTime
		created
		updated
		placeType
		population
		parent {
			id
			name
			description
			imageIds
			thumbnailId
			created
			updated
			placeType
			population
			parent {
				id
				name
				description
				imageIds
				thumbnailId
				created
				updated
				placeType
				population
			}
		}
	}
}
